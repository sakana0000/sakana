'use client'
import { Title } from '@/components/Title'
import { Section } from '@/components/Section'

export function Skill() {
    return (
        <Section id="skill">
            <Title text="スキル" />
            <p>
                JavaScript / CSS / HTML / ESLint / Prettier / Storybook /
                styled/components / Material UI / React Query / Typescript /
                React / Next.js / jest / cypress / React Testing Library / dart
                / Flutter / Confluence / Jira / Notion / Miro / Slack / VScode /
                Github / Github Actions / Figma
            </p>
        </Section>
    )
}
